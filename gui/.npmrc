# openapi-typescript 7 declares TypeScript 5 as a peer but only consumes its
# compiler API during generation; the generated output is verified by the
# repository's TypeScript 6 build. Keep npm ci aligned with that tested setup.
legacy-peer-deps=true
