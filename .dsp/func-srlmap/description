source: src/dvbfixer/pdbutils.py
kind: function
purpose: build_serial_map(pdb_path) / append_before_end(path, lines) / remap_conect_records: CONECT preservation across PDB rewrites via (chain, resid, atomname) identity (NOT serial numbers).
