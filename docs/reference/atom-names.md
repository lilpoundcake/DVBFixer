# `dvbfixer atom-names` — CLI reference

> This page is generated by `scripts/gen_cli_reference.py` from
> the subcommand's `parse_args()`. Do not edit by hand.
> For prose about how to use `atom-names`, see [`docs/commands/atom-names.md`](../commands/atom-names.md).

```
usage: dvbfixer atom-names [-h] -o OUTPUT [--variant-overrides JSON]
                           [--report-json JSON] --target-ff {amber,charmm}
                           [--profile {gromacs}] [--dry-run] [-v]
                           [--log-file PATH]
                           input

Convert PDB atom and residue names without modifying the source file.

options:
  -h, --help            show this help message and exit

Input / output:
  input                 Input legacy PDB file (.pdb or .ent)
  -o OUTPUT, --output OUTPUT
                        Output PDB file
  --variant-overrides JSON
                        JSON array of exact chain/residue/insertion-code
                        variant overrides
  --report-json JSON    Write a machine-readable JSON report

Naming conversion:
  --target-ff {amber,charmm}
                        Target force-field naming family
  --profile {gromacs}   Target consumer naming profile
  --dry-run             Validate and report without writing output

Diagnostics:
  -v, --verbose         Print conversion summary

Global logging:
  --log-file PATH       Append all stdout/stderr (including child tools) to
                        PATH while still printing it

Batch mode:
  This command does not support directory batch input. Run it once per
  input, or use a supported pipeline command.
```
